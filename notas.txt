python main.py                   ---ejecutar
.\.venv\Scripts\activate         ---activar venv

python -m tests.probar_estado_manager

"Continuemos el proyecto listar_corel.
Ya tenemos AnalizadorPiezas, PedidoManager, EstadoManager y ResumenProduccion funcionando. 
Vamos a empezar con ui/menu.py."


E:\alejandra\automatizacion_uniformes                     ---ubicacion carpetas
E:\alejandra\automatizacion_uniformes\nuevo pedido        ---ubicacion pedido

rm -r .venv                      ---borrar entorno
python -m venv .venv             ---crea entorno
py -0                            ---ver versione python
deactivate
Remove-Item -Recurse -Force .venv

python3 -m pip install pywin32


git switch checkpoint-texturas-estable-logo-inestable     ----proyecto estable


pip freeze > requirements.txt    ---clona librerias
pip install -r requirements.txt  ---sincroniza las librerias

git --version                     ---verificar si hay git
git init                          ---iniciar repo 

.gitignore                        ---crear gitignore

# entorno virtual
.venv/

# cache python
__pycache__/
*.pyc

# vscode
.vscode/

# backups
*.bak

# logs
*.log                       ---guarda esto


New-Item .gitignore         ---crea gitignore
notepad .gitignore          ---y pegas contenido

git status                  ---verificar que no este venv y estado

git init
git add .
git commit -m "nuevo nombre"

